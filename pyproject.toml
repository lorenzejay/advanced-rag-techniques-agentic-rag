[project]
name = "pre-processing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crewai>=0.102.0",
    "datasets>=3.3.2",
    "docling>=2.24.0",
    "sentence-transformers>=3.4.1",
]
